---
    - name: Playbook the queries web service
      hosts: localhost
      tasks:
        - name: Ensure the webserver is reachable
          uri:
            url: http://ansiblelab1
            status_code: 200 
...